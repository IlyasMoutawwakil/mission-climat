<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.missionclimat">

    <application
        android:allowBackup="true"
        android:icon="@drawable/planet"
        android:label="@string/appName"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:roundIcon="@drawable/planet"
        android:supportsRtl="true">

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name="com.example.missionclimat.AccueilActivity">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN" />
                <category
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.missionclimat.CategoriesActivity" />

        <activity
            android:name="com.example.missionclimat.MissionActivity" />

        <activity
            android:name="com.example.missionclimat.EmpreinteActivity"/>

    </application>

</manifest>